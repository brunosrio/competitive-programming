#include <bits/stdc++.h>
using namespace std;
#define ll long long;

int main() {
  cin.tie(0)->sync_with_stdio(0);
  int n, m; cin >> n >> m;
  vector<string> matrix(n);
  for (int i = 0; i < n; i++) {
    string l; cin >> l;
    matrix[i] = l;
  }

}

